pipeline {
    agent {label 'docker' }
    stages {
        stage('build') {
            steps {
                sh "echo 'chorizonnnnn!!!!!'"
                sh 'mvn --version'
            }
        }
    }
}